apiVersion : v1
kind: PersistentVolumeClaim
metadata:
  name: example-pvc
spec:
  storageClassName : standard
  volumeMode : Filesystem
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
    - ReadWriteMany
